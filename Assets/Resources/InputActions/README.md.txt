# Input Actions folder

### Notes
1. InputSystem_Actions is the default unity input actions file. I am no longer using it
2. GameInputActions is the currently used input actions file used project-wide.